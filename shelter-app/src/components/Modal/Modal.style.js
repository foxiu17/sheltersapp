import styled from "styled-components";

// import Button from "@material-ui/core/Button";
import UI_DIALOG from "@material-ui/core/Dialog";
import UI_DIALOG_ACTIONS from "@material-ui/core/DialogActions";
import UI_DIALOG_CONTENT from "@material-ui/core/DialogContent";
import UI_DIALOG_CONTENT_TEXT from "@material-ui/core/DialogContentText";
import UI_DIALOG_TITLE from "@material-ui/core/DialogTitle";

import { Button as commonButton } from "../../assets/common/Button.style";

export const Dialog = styled(UI_DIALOG)``;

export const DialogActions = styled(UI_DIALOG_ACTIONS)``;

export const DialogContent = styled(UI_DIALOG_CONTENT)``;

export const DialogContentText = styled(UI_DIALOG_CONTENT_TEXT)``;

export const DialogTitle = styled(UI_DIALOG_TITLE)``;

export const Button = styled(commonButton)``;
